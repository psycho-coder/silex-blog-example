{% extends "form_div_layout.html.twig" %}

{% block form_errors %}
{% spaceless %}
    {% if errors|length > 0 %}
        <div class="alert alert-danger">{{ parent() }}</div>
    {% endif %}
{% endspaceless %}
{% endblock %}